services:
  - type: web
    name: moh-document-intelligence
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app/main_ui.py --server.port $PORT --server.address 0.0.0.0
